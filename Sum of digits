#include <stdio.h>

int main()
{
    int T,N,i,r,sum=0;
    scanf("%d",&T);
    for(i=1;i<=T;i++)
    {
       scanf("%d",&N);
       while(N>0)
      {
        r=N%10;
        sum=sum+r;
        N=N/10;
      }
       printf("%d\n",sum);
       sum=0;
    }
}
