#include <stdio.h>

int main(void) {
	int N,T,i,fact=1;
	scanf("%d",&T);
	while(T--)
	{
	    scanf("%d",&N);
	    for(i=N;i>=1;i--)
	    {
	    fact=fact*i;
	    }
	    printf("%d\n",fact);
	    fact=1;
	}
	return 0;
}
